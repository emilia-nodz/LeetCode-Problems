/**
 * @param {...(null|boolean|number|string|Array|Object)} args
 * @return {number}
 */
var argumentsLength = function(...args) {
    let counter = 0;
    args.forEach((element) => counter++);
    return counter;
};

/**
 * argumentsLength(1, 2, 3); // 3
 */
